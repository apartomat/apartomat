extend type Mutation {
    acceptInvite(token: String!): AcceptInviteResult!
}

union AcceptInviteResult = InviteAccepted | InvalidToken | ExpiredToken | ServerError

type InviteAccepted {
    token: String!
}
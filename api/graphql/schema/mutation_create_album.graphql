extend type Mutation {
    createAlbum(projectId: String! name: String!): CreateAlbumResult!
}

union CreateAlbumResult = AlbumCreated | Forbidden | ServerError

type AlbumCreated {
    album: Album!
}
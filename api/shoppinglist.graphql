extend type Query {
    shoppinglist: ShoppinglistQuery!
}

type ShoppinglistQuery {
    productOnPage(url: String!): Product
}

type Product {
    name: String!
    description: String!
    image: String!
}
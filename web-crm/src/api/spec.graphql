query specScreen($projectId: String!) {
    screen {
        screen: spec(projectId: $projectId) {
            project {
                __typename
                ...on Project {
                    id
                    title
                }
                ...on Error {
                    message
                }
            }
        }
    }
}
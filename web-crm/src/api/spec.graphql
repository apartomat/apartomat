query specScreen($projectId: String!) {
    screen {
        specScreen: spec(projectId: $projectId) {
            project {
                __typename
                ...on Project {
                    id
                    title
                }
                ...on Error {
                    message
                }
            }
        }
    }
}
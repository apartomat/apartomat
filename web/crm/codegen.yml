overwrite: true
schema: "http://localhost:8010/graphql"
documents: "src/**/*.graphql"
generates:
  api/graphql.ts:
    plugins:
      - "typescript"
      - "typescript-operations"
      - "typescript-react-apollo"
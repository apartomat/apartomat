query specScreen($projectId: Int!) {
    screen {
        screen: spec(projectId: $projectId) {
            project {
                __typename
                ...on Project {
                    id
                    title
                }
                ...on Error {
                    message
                }
            }
        }
    }
}